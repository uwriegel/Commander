npm i @types/node
npm i express
npm i @types/express
npm i compression
npm i @types/compression
remove dependencies from package.json

build main thread: compile typescript with nwgui/tsconfig.json
build UI thread: compile typescript with typescript/tsconfig.json

build Background process: 
open vscode in folder ./processes/commander
to debug: debug and paste content of test.txt

to build: dotnet publish:
dotnet publish --framework netcoreapp2.0 -c release (dotnet publish --framework netcoreapp2.0 --self-contained)
copy content of /processes/commander/bin/release/netcoreapp2.0/publish to /processes/bin

